import { Store } from './store.js'; import { UI } from './ui.js'; import { escapeHtml, requireFields } from './utils.js';
export async function crmPage(){ const leads = await Store.select('leads'); const rows = leads.map(l=>`<tr><td>${escapeHtml(l.name)}</td><td>${escapeHtml(l.phone)}</td><td>${escapeHtml(l.status||'New')}</td><td>${escapeHtml(l.source||'Website')}</td></tr>`).join('') || '<tr><td colspan="4">No leads yet.</td></tr>'; return UI.shell(`<div class="grid gap-6 lg:grid-cols-3"><div class="lg:col-span-1">${UI.form([{name:'name',label:'Lead Name',required:true},{name:'phone',label:'Phone',required:true},{name:'source',label:'Source'},{name:'status',label:'Status'}],'Add Lead')}</div><div class="lg:col-span-2">${UI.table(['Name','Phone','Status','Source'], rows)}</div></div>`, 'crm'); }
export async function bindCrm(){ const form=document.getElementById('entity-form'); if(!form) return; form.addEventListener('submit', async e=>{ e.preventDefault(); const payload=Object.fromEntries(new FormData(form)); requireFields(payload,['name','phone']); await Store.insert('leads', payload); UI.toast('Lead captured'); location.reload(); }); }
