import { UI } from './ui.js';
export function dashboardPage() { return UI.shell(`<div class="grid gap-4 md:grid-cols-4">${UI.stat('Open Leads',18,'bullhorn')}${UI.stat('Installations',7,'screwdriver-wrench')}${UI.stat('Service Tickets',11,'headset')}${UI.stat('AMC Renewals',5,'shield-halved')}</div><div class="card mt-6 p-5"><h2 class="text-xl font-black">Business Lifecycle</h2><p class="mt-2 text-slate-600 dark:text-slate-300">Lead → Survey → Quotation → Order → Inventory → Installation → Invoice → Payment → Warranty → AMC → Service → Renewal.</p><div id="sales-chart" class="mt-4"></div></div>`, 'dashboard'); }
